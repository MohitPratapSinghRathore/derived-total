# Large regenerable artifacts only. Everything else is tracked.
#   data/  : ~1.5 GB of PGN dumps, oracle label arrays, and the Stockfish binary
#             (rebuild: code/prep_data.py then code/make_states.py)
#   runs/  : model checkpoints (rebuild: code/run_grid.py)
#   tools/ : downloaded tectonic binary
data/
runs/
tools/
*.aux
*.bbl
*.blg
*.out
build*/log.txt
smoke.log
